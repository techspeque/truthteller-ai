# TruthTeller AI v0.3.1

Released: 2026-03-29

## Fixes

- Fixed Windows release bundling by explicitly including the `.ico` asset in `backend/tauri.conf.json`, which Tauri requires for MSI and NSIS packaging.

## Chore

- Kept the cross-platform bundle icon list explicit by including both macOS (`.icns`) and Windows (`.ico`) icon formats alongside the PNG assets.
